/**
 * PetPickerOverlay — bottom sheet listing pets + "Tạo pet mới" +
 * "Không gán", shown when the chip is tapped (Design §4.1, DL-F02-06).
 *
 * Skeleton for F02 task 1.2; full list lands in later tasks.
 */

import React from 'react';
import { Text, View } from 'react-native';

const PetPickerOverlay: React.FC = () => {
  return (
    <View testID="pet-picker-overlay">
      <Text>Pet Picker</Text>
    </View>
  );
};

export default PetPickerOverlay;

/**
 * EditPetProfileSheet — bottom sheet to edit an existing pet
 * (Design §4.1).
 *
 * Skeleton for F02 task 1.2; full form lands in later tasks.
 */

import React from 'react';
import { Text, View } from 'react-native';

const EditPetProfileSheet: React.FC = () => {
  return (
    <View testID="edit-pet-profile-sheet">
      <Text>Edit Pet Profile</Text>
    </View>
  );
};

export default EditPetProfileSheet;

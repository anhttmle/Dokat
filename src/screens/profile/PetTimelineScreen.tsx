/**
 * PetTimelineScreen — infinite-scroll photo timeline of one pet
 * (Design §4.1).
 *
 * Skeleton for F02 task 1.2; full layout lands in later tasks.
 */

import React from 'react';
import { Text, View } from 'react-native';

const PetTimelineScreen: React.FC = () => {
  return (
    <View testID="pet-timeline-screen">
      <Text>Pet Timeline</Text>
    </View>
  );
};

export default PetTimelineScreen;

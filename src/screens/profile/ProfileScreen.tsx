/**
 * ProfileScreen — owner profile + pet profiles list (Design §4.1).
 *
 * Skeleton for F02 task 1.2; full layout lands in later tasks.
 */

import React from 'react';
import { Text, View } from 'react-native';

const ProfileScreen: React.FC = () => {
  return (
    <View testID="profile-screen">
      <Text>Profile</Text>
    </View>
  );
};

export default ProfileScreen;
